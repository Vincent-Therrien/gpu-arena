# Computing Example with OpenGL

## Build

This only works on Linux! Trying to compile on Windows is a waste of time!

```
sudo apt update && sudo apt install -y cmake g++ libglew-dev libgl1-mesa-dev xorg-dev
mkdir build
cd build
cmake ..
cmake --build .
./computing
```
